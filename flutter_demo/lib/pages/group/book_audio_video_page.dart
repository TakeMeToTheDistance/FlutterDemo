import 'package:flutter/material.dart';

class BookAudioVideoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("BookAudioVideoPage"),
        ),
        body: Center(child: Text('BookAudioVideoPage')));
  }
}
